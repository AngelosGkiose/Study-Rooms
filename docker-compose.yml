version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: studyrooms-db
    environment:
      POSTGRES_DB: studyrooms_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"